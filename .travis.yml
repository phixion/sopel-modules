language: python
python:
  - '2.7'
  - '3.5'
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libenchant-dev redis-server
install: "pip install -e ."
script:
   - pycodestyle --show-source --ignore=E501,E402,E722  ./